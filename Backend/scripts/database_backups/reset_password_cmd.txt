$admin = \App\Models\Admin::where('email', 'admin@bakery.com')->first();
if ($admin) {
    $admin->password = \Illuminate\Support\Facades\Hash::make('admin123');
    $admin->save();
    echo "Password reset successful for " . $admin->email;
} else {
    echo "Admin user not found.";
}
exit();
